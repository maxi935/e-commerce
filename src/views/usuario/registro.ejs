<!DOCTYPE html>
<html lang="en">
<!--HEAD-->
<%- include('../partials/head', {titulo : 'Registro' , estilo : 'registro.css' }) %>


  <body>
    <!--Header-->
    <%- include('../partials/header.ejs')%>
      <!--Main-->
      <main class="body-registro">


        <div class="form-registro">
          <form action="/usuario/registro" method="post" class="e-form">
            <h1>Formulario de Registro</h1>
            <div class="input-box">
              <label for="nombre">Nombre/s</label>
              <input type="text" class="e-nombre" value="<%= locals.old && old.nombre ? old.nombre : null %>"
                placeholder="Ingrese su Nombre" name="nombre">
              <% if (locals.errors && errors.nombre) { %>
                <div class="text-danger">
                  <%= errors.nombre.msg %>
                </div>
                <% } %>
            </div>
            <div class="input-box">
              <label for="apellido">Apellido/s</label>
              <input type="text" class="e-apellido" placeholder="Ingrese su Apellido" name="apellido"
                value="<%= locals.old && old.apellido ? old.apellido : null %>">
              <% if (locals.errors && errors.apellido) { %>
                <div class="text-danger">
                  <%= errors.apellido.msg %>
                </div>
                <% } %>
            </div>
            <div class="input-box">
              <label for="dni">D.N.I.</label>
              <input type="text" placeholder="45234234" name="dni" class="e-dni">
            </div>
            <div class="input-box">
              <label for="celular">Celular</label>
              <input type="text" placeholder="3855567984" name="celular" class="e-celular">
            </div>
            <div class="input-box">
              <label for="direccion">Direccion</label>
              <input type="text" placeholder="belgrano sur N° 1200" name="direccion" class="e-direccion">
            </div>
            <div class="input-box">
              <label for="email">Correo Electrónico</label>
              <input type="email" class="e-email" placeholder="martin@gmail.com" name="email"
              value="<%= locals.old && old.email ? old.email : '' %>">
              <% if (locals.errors && errors.email) { %>
                <div class="text-danger">
                  <%= errors.email.msg %>
                </div>
                <% } %>
              </div>
              <div class="input-box">
                <label for="password">Contraseña</label>
                <input type="password" class="e-password" placeholder="Contraseña" name="password"
                  value="<%= locals.old && old.password ? old.password : '' %>">
                <% if (locals.errors && errors.password) { %>
                  <div class="text-danger">
                    <%= errors.password.msg %>
                  </div>
                  <% } %>
              </div>
              <div class="input-box">
                <label for="password2">Repetir Contraseña</label>
                <input type="password" class="e-password2" placeholder="Repetir Contraseña" name="password2"
                  value="<%= locals.old && old.password2 ? old.password : null %>">
                  <% if (locals.errors && errors.password2) { %>
                    <p class="text-danger"><%= errors.password2.msg %></p>
                <% } %>
                
              </div>
            <div class="input-box">
              <label for="fecha_nac">Fecha de nacimiento</label>
              <input type="date" placeholder="Fecha de nacimiento" name="fecha_nac" class="fecNac e-fecha_nac"
                value="<%= locals.old && old.fecha_nac ? old.fecha_nac : null %>">
              <% if (locals.errors && errors.fecha_nac) { %>
                <div class="text-danger">
                  <%= errors.fecha_nac.msg %>
                </div>
                <% } %>
            </div>
            <input type="hidden" name="action" value="registro">
            <button type="submit" class="boton-registro">Registrarme</button>
          </form>
        </div>
        <!-- <script src="/js/errors-forms-registro.js"></script> -->
      </main>

      <!--Footer-->
      <%- include('../partials/footer.ejs')%>
  </body>

</html>